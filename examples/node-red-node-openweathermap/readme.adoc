= Summary

This is an example of using openweathermap node (aka node-red-node-openweathermap) as hello world.

== Environment

As usual:

[source,bash]
----
$ docker-compose up -d
$ docker-compose down
----

